# URL de l'API backend
# En développement local (npm run dev) : pointe directement vers le backend
# En production Docker : Nginx proxifie /api → backend:80, laisser à /api
VITE_API_URL=/api

# Pour le dev local sans Docker (si le backend tourne sur localhost:5000) :
# VITE_API_URL=http://localhost:5000
